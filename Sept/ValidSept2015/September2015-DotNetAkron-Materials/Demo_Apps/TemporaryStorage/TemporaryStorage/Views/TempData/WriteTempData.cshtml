@model TemporaryStorage.Models.Contact
@{
    ViewBag.Title = "WriteTempData";
}

<h2>WriteTempData</h2>

@using (Html.BeginForm())
{
    <p>
        <label>Name</label>
        @Html.TextBoxFor(c => c.Name)
    </p>
    <p>
        <label>Email</label>
        @Html.TextBoxFor(c => c.Email)
    </p>
    <div><button type="submit">Store In Tempdata</button></div>
}
